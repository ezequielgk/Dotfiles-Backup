# Mango Customization Rules

1. **Strict Documentation Compliance**: Every change, parameter, or syntax added to the Mango configuration files MUST be verified against the official documentation located locally in `docs/` (e.g. `~/.config/mango/docs/mango`). Do not guess parameters or borrow syntax from other window managers (like Hyprland) without confirming it in the provided documentation first.
